
See `galaxian.html`. It is a standalone html file that emulates classic arcade game Galaxian (1979).
See `defender.html`. It is a standalone html file that emulates classic arcade game Defender (1981).
See `starforce.html`. It is a standalone html file that emulates classic arcade game Star Force (1984).

See `index.html`. It is an 80's arcade, with the machines (cabinets): Galaxian, Defender, Star Force.
In the header topbar, there is a link to complexity.zone, and a crumb to arcade.

I want this link to be in the game html files.
In each of the game html file (`galaxian.html`, `defender.html`, `starforce.html`), in the header topbar, prepend a link to complexity.zone and arcade.
This allows user to navigate to complexity.zone and to arcade (`index.html`).

